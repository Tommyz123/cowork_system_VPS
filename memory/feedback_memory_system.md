---
name: feedback_memory_system
description: 主公对记忆系统的核心偏好：按需检索、严格写入标准，垃圾进垃圾出
type: feedback
originSessionId: bf4a9b7a-bea8-4e93-855f-c6cc59ac7344
---
主公明确反对全量加载所有记忆文件，认为是 token 浪费。
主公同样反对滥写记忆——乱写会让数据质量变差，记忆失去价值。

**核心需求：** 按需检索 + 严格写入标准。

**Why:** 记忆的价值在于"有用时能准确调用"，乱写导致噪音增加，反而干扰判断。

**How to apply — 读取：**
- 不要在会话开始就读取所有 memory 文件
- MEMORY.md 的摘要足够做第一层判断，需要深度细节时再读具体文件
- 未来 memory 文件超30个时可评估 mini RAG 路由

**How to apply — 写入（两层过滤）：**

**第一层：快速拒绝（遇到这些直接找对应位置）**
- 是待实现想法 → BACKLOG
- 是当前进度/状态 → CURRENT_SESSION.md
- 代码/文件里能读到 → 不重复
- 模糊愿望（"以后改进X"）→ 太虚，不写

**第二层：5个问题（越多Yes越应该写，少于3个不写）**
① **持久性** — 1个月后还准确吗？（状态会变的不写）
② **跨对话价值** — 新开对话的 Claude 不知道会吃亏吗？
③ **改变行为** — Claude 知道后会做不同的事吗？
④ **不可推导** — 无法通过读现有文件推出来吗？
⑤ **够具体** — 具体到能指导行为，而不是泛泛方向？

**不同类型的侧重：**
- feedback → 重点看③（改变行为），最高标准
- project → 重点看②④（跨对话价值 + 不可推导）
- reference → 重点看②⑤（跨对话 + 具体路径）
- user → 重点看①③（持久 + 影响沟通方式）

---

## 决策案例（积累后可自主判断）

**2026-04-20**
✅ feedback/多agent态度 → 影响未来提方案的方式，每次讨论架构都会用到
❌ project/Cowork瓶颈判断 → 当下情境判断，两周后会变，可从项目状态推导
✅ project/P9反馈闭环架构 → 记录了"为什么这样设计"的决策原因，推导不出来
✅ feedback/CLI优先原则 → 主公明确说"这是原则"，直接改变提方案的方式

**2026-04-25**
✅ feedback/记忆系统边界确认 → memory/只记路由指针，project_*.md不存项目内容（按需读取，不自动加载）
